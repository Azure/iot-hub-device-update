name: CI
on:
  push:
  workflow_dispatch:
jobs:
  container-job:
    strategy:
      fail-fast: false
      matrix:
        os: ["debian:12", "debian:11", "ubuntu:22.04", "ubuntu:20.04"]
        arch: [amd64, arm64]
    runs-on: ${{ matrix.arch == 'arm64' && 'ubuntu-22.04-arm' || 'ubuntu-latest' }}
    container:
      image: ${{ matrix.os }}
      options: --platform linux/${{ matrix.arch }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Make artifact name valid
        id: make-artifact-name-valid
        run: |
          OS_NAME=$(echo "${{ matrix.os }}" | tr ':' '-')
          echo "os_name=$OS_NAME" >> $GITHUB_OUTPUT
      - name: Install prereqs
        run: |
          apt-get update
          DEBIAN_FRONTEND=noninteractive apt-get install -y tzdata
          dpkg-reconfigure -f noninteractive tzdata
      - name: Install dependencies
        run: |
          if [[ "${{ matrix.os }}" == "ubuntu:22.04" ]] || [[ "${{ matrix.os }}" > "ubuntu:22.04" && "${{ matrix.os }}" == ubuntu:* ]]; then
            scripts/install-deps.sh --install-aduc-deps --install-do --install-cmake --install-shellcheck --install-valgrind auto
          else
            scripts/install-deps.sh --install-aduc-deps --install-do --install-cmake --install-shellcheck
          fi
      - name: Build packages and tests
        run: scripts/build.sh --clean --build-unit-tests --build-packages --type MinSizeRel
      - name: Run tests
        run: |
          cd out
          ctest
      - name: Run tests with Valgrind (memory leak check)
        if: matrix.os == 'ubuntu:22.04' || startsWith(matrix.os, 'ubuntu:2') && matrix.os >= 'ubuntu:22.04'
        run: |
          cd out
          ctest -T memcheck --output-on-failure
        continue-on-error: true
      - name: Upload Valgrind results
        if: always() && (matrix.os == 'ubuntu:22.04' || startsWith(matrix.os, 'ubuntu:2') && matrix.os >= 'ubuntu:22.04')
        uses: actions/upload-artifact@v4
        with:
          name: valgrind-results-${{ steps.make-artifact-name-valid.outputs.os_name }}-${{ matrix.arch }}
          path: out/Testing/Temporary/MemoryChecker.*.log
          retention-days: 7
          if-no-files-found: ignore
      - name: Upload Debian packages
        uses: actions/upload-artifact@v4
        with:
          name: deb-packages-${{ steps.make-artifact-name-valid.outputs.os_name }}-${{ matrix.arch }}
          path: out/*.deb
          retention-days: 3
