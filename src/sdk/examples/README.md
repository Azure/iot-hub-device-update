# Build and Run Examples

## Build

```sh
make clean
make all
```

## Run

```sh
sudo -u adu ./simple_status_check -v
sudo -u adu ./status_monitor
<Ctrl-C>
```

NOTE: It is important to use `sudo -u adu ` on the front to run as adu user.
